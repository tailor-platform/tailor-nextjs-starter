query employees {
  employees {
    collection {
      id
      employeeCode
      firstName
      lastName
    }
  }
}

mutation login($orgKey: String!, $id: String!, $password: String!) {
  login(orgKey: $orgKey, id: $id, password: $password) {
    token
  }
}
